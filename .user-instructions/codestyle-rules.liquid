# `Codestyle Rules`

- Prefer String.raw when working with strings that contain backslashes
- Do not use curly braces {} for single-line if/else bodies
- Add short single-line // comments in important places of any code you generate or modify
- Do not use triple backticks in a row for TS/JS files; instead, split them like '`'+'``' to avoid sanitization issues